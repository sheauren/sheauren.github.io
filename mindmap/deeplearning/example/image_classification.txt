image classification
    dataset:fashion_mist
        train_images
            (60000,28,28)
        train_labels
            (60000,10)
        valid_dataset                            
            (10000,28,28)
        valid_labels
            (10000,10)
    classes
        10
    model
        Flatten
        Dense(128)/relu
        Dense(10)/softmax
    loss_function
        sparse_categorical_crossentropy
    optimizer
        AdamOptimizer
    metric
        accuracy
    fit # train
        train_images,train_labels
        epochs
            5
        batch_size                
    evaluate
        test_images,test_labels
        test_loss
        test_acc
    predict
        predict_classes=np.argmax(predict[:,:],axis=1])